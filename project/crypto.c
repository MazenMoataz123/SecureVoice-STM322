#include "crypto.h"
#include "transport.h" // Assuming SecurePacket_t is defined here

void crypto_init(void) {
    // Future Ascon init logic
}

void encrypt_packet(SecurePacket_t *packet) {
    // Placeholder: No encryption yet
    (void)packet;
}

void decrypt_packet(SecurePacket_t *packet) {
    // Placeholder: No decryption yet
    (void)packet;
}